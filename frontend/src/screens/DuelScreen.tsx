import { useState } from 'react'
import type { Card, DuelState, PlayerId, Zone } from '../types'
import { CardDetail } from '../components/CardDetail'
import { CardRow } from '../components/CardRow'
import { LpPanel } from '../components/LpPanel'
import { PhaseBar } from '../components/PhaseBar'
import { ZoneTabs } from '../components/ZoneTabs'
export function DuelScreen({ duel, onNext, onLp, onMove }: { duel: DuelState; onNext: () => void; onLp: (p: PlayerId, amount: number) => void; onMove: (p: PlayerId, from: Zone, to: Zone, id: number) => void }) { const [player, setPlayer] = useState<PlayerId>('p1'); const [zone, setZone] = useState<Zone>('deck'); const [card, setCard] = useState<Card | null>(null); const active = duel.players[player]; return <main className="screen duel-screen"><PhaseBar turn={duel.turn} player={duel.activePlayer} phase={duel.phase} onNext={onNext} /><LpPanel p1={duel.players.p1.lp} p2={duel.players.p2.lp} history={duel.lpHistory} onChange={onLp} /><div className="player-switch"><button className={player === 'p1' ? 'active' : ''} onClick={() => setPlayer('p1')}>Joueur 1</button><button className={player === 'p2' ? 'active' : ''} onClick={() => setPlayer('p2')}>Joueur 2</button></div><ZoneTabs zones={active.zones} selected={zone} onChange={setZone} /><section className="zone-content"><div className="section-title"><h2>{zone === 'hand' ? 'Main' : zone === 'field' ? 'Terrain' : zone === 'graveyard' ? 'Cimetière' : zone === 'banished' ? 'Banni' : zone[0].toUpperCase() + zone.slice(1)}</h2><span>{active.zones[zone].length} cartes</span></div>{active.zones[zone].length ? active.zones[zone].map((item, i) => <CardRow key={`${item.id}-${i}`} card={item} onClick={() => setCard(item)} />) : <p className="empty">Zone vide.</p>}</section>{card && <CardDetail card={card} zone={zone} onClose={() => setCard(null)} onMove={to => { onMove(player, zone, to, card.id); setCard(null) }} />}</main> }
