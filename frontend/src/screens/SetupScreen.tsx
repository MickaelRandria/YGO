import type { Deck } from '../types'
import { Play, Pencil } from 'lucide-react'
export function SetupScreen({ decks, onEdit, onStart }: { decks: Record<'p1' | 'p2', Deck>; onEdit: (player: 'p1' | 'p2') => void; onStart: () => void }) { const canStart = decks.p1.cards.length > 0 && decks.p2.cards.length > 0; return <main className="screen"><header className="brand"><div className="brand-mark">YR</div><div><h1>YGO Referee</h1><p>Configuration du duel</p></div></header><div className="setup-cards">{(['p1', 'p2'] as const).map((player, index) => { const deck = decks[player]; return <section className="setup-card" key={player}><div className="setup-head"><div><span>Joueur {index + 1}</span><strong>{deck.cards.length} carte{deck.cards.length !== 1 ? 's' : ''}</strong></div><button className="secondary" onClick={() => onEdit(player)}><Pencil size={16} />{deck.cards.length ? 'Modifier' : 'Construire'}</button></div><div className="card-preview">{deck.cards.slice(0, 5).map((card, i) => card.card_images?.[0] ? <img key={`${card.id}-${i}`} src={card.card_images[0].image_url_small} alt="" /> : <div key={i} />)}{!deck.cards.length && <p>Le deck sera conservé localement.</p>}</div></section> })}</div><button className="primary launch" disabled={!canStart} onClick={onStart}><Play size={20} /> Lancer le duel</button>{!canStart && <p className="hint">Ajoutez au moins une carte à chaque deck pour lancer le duel.</p>}</main> }
