import { ChevronRight } from 'lucide-react'
import type { Phase, PlayerId } from '../types'
const help: Record<Phase, string> = { Draw: 'Le joueur actif pioche 1 carte.', Standby: 'Résolution des effets « pendant la Standby Phase ». ', 'Main 1': 'Invocation normale (1/tour), poser des monstres, activer magies/pièges, invocations spéciales.', Battle: 'Déclarer des attaques. Pas au 1er tour du J1.', 'Main 2': 'Comme Main 1 (sauf invocation normale si déjà faite).', End: 'Effets « pendant la End Phase ». Défausser si >6 cartes en main.' }
export function PhaseBar({ turn, player, phase, onNext }: { turn: number; player: PlayerId; phase: Phase; onNext: () => void }) { return <div className="phase-wrap"><div className="phase-bar"><span className="turn">T{turn}</span><span className="player-pill">{player === 'p1' ? 'J1 actif' : 'J2 actif'}</span><button className="phase-name" title={help[phase]}>{phase}</button><button className="next-phase" onClick={onNext} aria-label="Phase suivante"><ChevronRight size={20} /></button></div><p className="phase-help">{help[phase]}</p></div> }
